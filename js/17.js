const numero1=20;
const numero2="20";

console.log(parseInt(numero2)); //parseInt() es una funcion porque llamamos al metodo seguido del parentesis
console.log(numero1.toString()); //.toString() es un metodo porque llamamos a la variable seguido de un . y despues el metodo
