// Objeto Math

let resultado;

resultado=Math.PI;
resultado=Math.round(2.5); //Redondea a un entero
resultado=Math.ceil(2.1); //Redondea hacia arriva
resultado=Math.floor(2.9); //Redondea hacia abajo
resultado=Math.sqrt(144); //Raiz cuadrada
resultado=Math.abs(-15); //Convierte a positivo el numero
resultado=Math.min(3,5,1,8,2,10); //Devuelve el numero mas menor de la lista
resultado=Math.max(3,5,1,8,2,10); //Devuelve el numero mas mayor de la lista
resultado=Math.random(); //Numero Aleatorio
resultado=Math.floor(Math.random()*30);

console.log(resultado);